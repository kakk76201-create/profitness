# Зависимости backend-части (Python 3.12+).
# Версии ЗАКРЕПЛЕНЫ точно: сборка на Railway должна давать тот же набор
# пакетов, что проверен тестами локально. Обновлять осознанно, парой
# «поднять версию → прогнать tests/run_all.py».
fastapi==0.137.2
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
openai==2.43.0
python-multipart==0.0.32
python-dotenv==1.2.2
pillow==12.2.0
pydantic==2.13.4
# Драйвер PostgreSQL для продакшена (Railway Postgres). На SQLite не влияет —
# подключается только когда DATABASE_URL начинается с postgresql://.
psycopg2-binary==2.9.12
# Планировщик фоновых задач (пуш-уведомления) и HTTP-клиент для Telegram Bot API.
apscheduler==3.11.2
httpx==0.28.1
# База часовых поясов IANA — нужна, чтобы APP_TZ (например, Europe/Moscow)
# корректно работал на Windows и в slim-контейнерах Railway (иначе откат на UTC).
tzdata==2026.2
# Мониторинг ошибок; включается переменной SENTRY_DSN, без неё не используется.
sentry-sdk[fastapi]==2.69.2
